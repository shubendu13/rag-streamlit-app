version: "3.8"

services:
  rag:
    build: .
    container_name: rag-container
    ports:
      - "8501:8501"
    command: streamlit run app.py --server.port=8501 --server.address=0.0.0.0
    restart: always
